# Slide-stitching pipeline — Python dependencies
#
#     pip install -r requirements.txt
#
# Scripts also auto-install missing packages at runtime via
# common.ensure_requirements(), but this file is the canonical list and lets
# you pre-install everything in one shot.

# --- core (stages 1-2: borders, cutting) ---
numpy>=1.24
opencv-python>=4.8

# --- matching (stage 3) ---
scikit-image>=0.21      # SSIM (metrics stage)

# PyTorch — required by the RoMa and hybrid matchers (MATCH_METHOD=roma|hybrid).
# Not needed for the default SIFT matcher, but installed by default here so all
# three methods work out of the box.
#
# NOTE: this pulls the DEFAULT torch build for your platform, which on many
# systems is CPU-only. For CUDA (GPU) acceleration, install the matching CUDA
# build instead — see requirements-roma.txt and the README. RoMa on CPU runs
# but is very slow.
# torch>=2.0

# --- stitching (stage 7) ---
tifffile>=2023.7        # pyramidal OME-TIFF output

# --- global optimization + error estimation (stages 6-8) ---
scipy>=1.10

# --- optional ---
# lpips>=0.1.4          # perceptual metric (stage 4), needs torch (already above)
# romav2                # RoMa dense matcher — see requirements-roma.txt
